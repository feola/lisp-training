
(defun golova (x y) (car sp))
(defun hvost (x y) (cdr sp))
(defvar sp (list x y))

; Как сделать, чтобы написав, например, > (golova 2 3)
; или > (hvost 2 3)  получить числа, а не x и (y)?
; Не решено.
