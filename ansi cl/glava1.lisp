;;---------------------------------------------------
;; Функция, складывающая все числа меньше n

(defun sum (n)
  (let ((s 0))
    (dotimes (i n s)
      (incf s i))))

;; CL-USER> (sum 8)
;; 28

;;---------------------------------------------------
;; Функция, которая принимает число n и возвращает
;; функцию, которая добавляет n к своему аргументу

(defun addn (n)
  #'(lambda (x)
      (+ x n)))

;; CL-USER> (addn 5)
;; #<CLOSURE (LAMBDA (X) :IN ADDN) {C6C354D}>

